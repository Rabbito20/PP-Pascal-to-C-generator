# PP-Pascal-to-C-generator
Simple implementation of the Pascal interpreter that translates and generates code to C language.
Wrtitten in Python.

Code is divided into:
- Lexer
- Parser
- C code generator
